name: ui-regression

on:
  workflow_dispatch:
  schedule:
    # nightly at 01:30 UTC
    - cron: "30 1 * * *"

jobs:
  regression:
    uses: ./.github/workflows/reusable-ui-tests.yml
    with:
      tag: regression
      headless: true
